MERN STACK DEPLOYMENT ON AZURE VM
WITH NGINX REVERSE PROXY
WITHOUT PM2
FRONTEND ACCESS USING PUBLIC_IP

====================================================
ARCHITECTURE
============

Browser
↓
Nginx Reverse Proxy :80
↓
Frontend React/Vite Application :3000
↓ API Requests
Backend Node/Express Server :5000
↓
MongoDB Database

====================================================
STEP 1 — CREATE RESOURCE GROUP
==============================

Azure Portal → Search:

Resource Groups

Click:

Create

Configuration:

Resource Group Name:
mern-rg

Region:
Central India

====================================================
STEP 2 — CREATE VIRTUAL MACHINE
===============================

Azure Portal → Search:

Virtual Machines

Click:

Create → Azure Virtual Machine

Configuration:

Resource Group:
mern-rg

VM Name:
vm1

Region:
Central India

Image:
Ubuntu Server 22.04 LTS

Size:
Standard_B1s

Authentication:
SSH Public Key

Username:
azureuser

Allow Inbound Ports:

22
80

Download:

vm1_key.pem

====================================================
STEP 3 — CONNECT TO VM
======================

chmod 400 vm1_key.pem

ssh -i vm1_key.pem azureuser@PUBLIC_IP

Example:

ssh -i vm1_key.pem [azureuser@20.xx.xx.xx](mailto:azureuser@20.xx.xx.xx)

====================================================
STEP 4 — UPDATE UBUNTU
======================

sudo apt update

sudo apt upgrade -y

====================================================
STEP 5 — INSTALL NODEJS
=======================

curl -fsSL [https://deb.nodesource.com/setup_20.x](https://deb.nodesource.com/setup_20.x) | sudo -E bash -

sudo apt install nodejs -y

node -v

npm -v

====================================================
STEP 6 — INSTALL GIT
====================

sudo apt install git -y

git --version

====================================================
STEP 7 — INSTALL NGINX
======================

sudo apt install nginx -y

sudo systemctl start nginx

sudo systemctl enable nginx

sudo systemctl status nginx

====================================================
STEP 8 — CLONE PROJECT
======================

git clone YOUR_GITHUB_REPO_LINK

Example:

git clone [https://github.com/username/blog-application.git](https://github.com/username/blog-application.git)

cd blog-application

====================================================
STEP 9 — BACKEND SETUP
======================

cd backend

npm install

nano .env

Example:

PORT=5000

MONGO_URI=YOUR_MONGODB_CONNECTION_STRING

SAVE:

CTRL + O
ENTER
CTRL + X

====================================================
STEP 10 — CONFIGURE BACKEND HOST
================================

nano server.js

Update:

app.listen(5000, "0.0.0.0", () => {
console.log("Server Running");
});

====================================================
STEP 11 — START BACKEND
=======================

npm start

OR

node server.js

IMPORTANT:

Keep this terminal open.

====================================================
STEP 12 — OPEN SECOND SSH TERMINAL
==================================

ssh -i vm1_key.pem azureuser@PUBLIC_IP

====================================================
STEP 13 — FRONTEND SETUP
========================

cd ~/blog-application/frontend

npm install

====================================================
STEP 14 — UPDATE FRONTEND API URL
=================================

Replace:

http://PUBLIC_IP:5000

OR

localhost:5000

with:

/api

Example:

axios.get("/api/posts")

====================================================
STEP 15 — START FRONTEND
========================

For Vite:

npm run dev -- --host 0.0.0.0

For Create React App:

HOST=0.0.0.0 npm start

IMPORTANT:

Keep this terminal open.

====================================================
STEP 16 — CONFIGURE NGINX REVERSE PROXY
=======================================

Open nginx configuration:

sudo nano /etc/nginx/sites-available/default

Delete everything and paste:

server {

```
listen 80;

server_name _;

location / {

    proxy_pass http://localhost:3000;

    proxy_http_version 1.1;

    proxy_set_header Upgrade $http_upgrade;

    proxy_set_header Connection "upgrade";

    proxy_set_header Host $host;

}

location /api/ {

    proxy_pass http://localhost:5000/;

    proxy_http_version 1.1;

    proxy_set_header Upgrade $http_upgrade;

    proxy_set_header Connection "upgrade";

    proxy_set_header Host $host;

}
```

}

SAVE:

CTRL + O
ENTER
CTRL + X

====================================================
STEP 17 — TEST NGINX CONFIGURATION
==================================

sudo nginx -t

Expected:

syntax is ok

test is successful

====================================================
STEP 18 — RESTART NGINX
=======================

sudo systemctl restart nginx

====================================================
STEP 19 — AZURE NETWORKING
==========================

Azure Portal → VM → Networking

Allow Inbound Rules:

22
80

Protocol:
TCP

Action:
Allow

====================================================
STEP 20 — ACCESS APPLICATION
============================

Frontend:

http://PUBLIC_IP

Example:

[http://20.xx.xx.xx](http://20.xx.xx.xx)

Backend API:

http://PUBLIC_IP/api

====================================================
IMPORTANT COMMANDS SUMMARY
==========================

Connect VM:
ssh -i vm1_key.pem azureuser@IP

Install Packages:
npm install

Start Backend:
npm start

Start Vite Frontend:
npm run dev -- --host 0.0.0.0

Restart Nginx:
sudo systemctl restart nginx

Check Nginx Status:
sudo systemctl status nginx

Test Nginx Config:
sudo nginx -t

Check Ports:
sudo ss -tulnp

====================================================
IMPORTANT NOTES
===============

Frontend and backend still stop if terminal closes.

Because PM2 is not being used.

Nginx only acts as reverse proxy.

It forwards:

/       → Frontend Port 3000
/api    → Backend Port 5000
